name: Continuous Integration

on:
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Check for syntax errors
        run: |
          echo "PR 코드 문법/형식 검사 예제"
